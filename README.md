
  # Senior Connect App Design

  This is a code bundle for Senior Connect App Design. The original project is available at https://www.figma.com/design/YPERRAOTqJbDVyezD806CN/Senior-Connect-App-Design.

  # Project Setup Guide
  1. Clone Repo
     Run `git clone`
  2. Install Dependencies
     Run `npm install` to install the dependencies.
  3. Start Development Server
     Run `npm run dev` to start the development server.

  Before starting work
  Run `git pull origin main
  Finishing work
  `git add .`
  `git commint -m "Your message"`
  `git push`
  
